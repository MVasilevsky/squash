#import(mvas.squash.Tag)

<%@ val tags: List[Tag] %>

<ul>
#for (tag <- tags)
    <li><%= tag.name %> (id=<%= tag.id %>) <a href="/tag/remove/<%= tag.id %>">x</a></li>
#end
</ul>

<a href="/tag/add">Add tag</a>
